##### My (demuredemeanor) configs for ma.sdf.org
# vim:set syntax=sh:
# Uses tabstop=4; shiftwidth=4 tabs; foldmarker={{{,}}};
# https://tildegit.org/demure/dotfiles
# legacy https://notabug.org/demure/dotfiles
# legacy repo http://github.com/demure/dotfiles

export PATH=/usr/pkg/bin:/usr/local/bin:/usr/bin:/bin
export TZ=EST5EDT
#source /etc/bash_completion.d/git      ## Add git completion

unalias svi 2>/dev/null
