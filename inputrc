##### My (demuredemeanor) .inputrc
# Uses shiftwidth=4 tabs
# http://github.com/demure/dotfiles

### Main Settings ###
	### Completion Stuffs ###
	set completion-ignore-case on	## Tab complete ignores case
	set show-all-if-ambiguous on	## Complete longest explicit match, then print remaining values
	set completion-map-case on		## With ignore on, will ignore diff between '-' and '_'
	set page-completions off		## Disable more-like pager of completion list
	"\t": menu-complete				## Steps through list, can give n first
	
	## Shift-TAB to menu-complete backwards
	# cf. http://www.tikirobot.net/wp/2006/03/29/reverse-menu-complete-in-bash/
#	\C-y:"\M--\C-i"

	set visible-stats on			## Append symbol for file, in tab completion
	### End Completion Stuffs ###

	### Keyboard ###
	"\e[3~": delete-char
	### End Keyboard ###

	### History Stuffs ###
	## Ended up disabling these as the prompt got a bit wonky
#	set mark-modified-lines on
#	set history-preserve-point on
	### End History Stuffs ###

	### Editing Mode ###
	## I prefer some of the emacs key bindings since OS X uses them.
#	set editing-mode vi
#	set keymap vi-insert
#	vi-command vi-insert
	### End Editing Mode ###

	### For Other Languages ###
	set meta-flag on		## Enable 8 bit input (doesn't clear eighth bit in chars it reads)
	set input-meta on		## Same flag...
	set output-meta on		## Display chars' eighth bit set directly, instead of esc sequence
	set convert-meta off	## Prevent 8th bit strip
	### End Other Languages ###
### End Main Settings ###

### Testing ###
	## Add the missing slash when tab-completing symbolic links to directories
#	set mark-symlinked-directories on
### End Testing ###
